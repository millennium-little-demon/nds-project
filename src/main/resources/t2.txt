#namespace com.ck.mapper.UserInfoMap_per

#metadata {
    $queryTimeout: "10s"
    $queryTimeout2: "101s"
}

 
    
     //:userName?.isNotNull()
    // :userName?.isNotNull(31, "1", true, null)
   //  &userName?.isNotNull()
         &userName?.isNotNull(31, "1", false, null)

    
    select * from user_info
    
    where user_name = &userName?.isNotNull
    where user_name = :userName -> :userName?.isNotNull
    
    
    #if :userName?.isNotNull(1, "a", true, null) {
    
    }
